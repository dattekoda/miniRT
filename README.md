## miniRT

## 11/14
`.rt`ファイルだけを読み込むように、文字列の接尾辞を比較する`minirt_strrcmp()`関数が完成。   

## 11/16
`.rt`ファイルから読み込んだ内容を`char *content`に保存する機能の実装   
`minilibx_macos`はホームディレクトリに保存してある前提に`Makefile`を編集   
`utils_validate.c`: ambientのvalidateが完成   

`strtof.c`を実装する必要あり。   
`fabs.c`を実装する必要あり。   

`validate_camera.c`: 作成途中、doubleの値を比較する関数が必要。   
